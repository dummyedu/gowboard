// const QuestionType = require('../../common/index');

const addOrInsertBoard = (board, feature) => {

};

const updateBoard = (board, feature) => {

};

const queryBoard = (condition, count, isRandom) => {

};

module.exports = {
  addOrInsertBoard,
  updateBoard,
  queryBoard,
};
